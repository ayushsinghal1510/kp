[project]
name = "postab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "ayushsinghal1510@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.33.0",
    "streamlit>=1.55.0",
]

[project.scripts]
postab = "postab:main"

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
